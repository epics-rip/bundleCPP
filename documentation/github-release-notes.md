EPICS V4 C++ Package

This release of the EPICS V4 C++ modules contains ...

More information is available in the bundle's [Release Notes
document](https://github.com/epics-base/bundleCPP/blob/release/4.x/documentation/RELEASE_NOTES.md).

Note that the two "Source code" download files below only contain the top-level
bundleCPP project files without any of the sub-modules themselves (the "Source
code" files are created automatically by github and can't be suppressed).

The EPICS-CPP-4.x.0 files below contain all the module sources.
